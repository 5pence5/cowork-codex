---
description: Show delegated Codex job status
argument-hint: "[job-id] [--wait <seconds>]"
---

Call `codex_job_status`. If a job id is provided, pass it as `id`; otherwise show active and recent delegated Codex jobs. Use bounded `wait_seconds` instead of tight polling loops.
