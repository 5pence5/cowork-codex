---
description: Cancel a delegated Codex job
argument-hint: "<job-id>"
---

Call `codex_cancel_job` for the job id. Report the resulting job status and do not call `codex_job_result` unless the user asks.
